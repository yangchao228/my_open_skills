# Minimal Input

请把这份已经审核通过的 8 页卡片包制作成小红书成图。

- `delivery_mode`: `publish_ready`
- `platform_profile`: `xiaohongshu`
- `asset_url_policy`: `local`
- `generation_backend`: `auto`
- 视觉要求：统一暖白底、深色文字和蓝色强调

请先选择确定性、生成式或混合式方案。需要生成式视觉时，自动检查可用后端；如果进入 ChatGPT Handoff，导回本地文件后再继续排版和逐图 QA。请生成真实的 1080×1440 PNG，输出 `cards-manifest`，停在人工确认和平台上传之前。
