请把下面这篇长文改成 8 张图文卡片，并继续生成可以上传的小红书图片。

- `delivery_mode`：`publish_ready`
- `platform_profile`：`xiaohongshu`
- `asset_url_policy`：`local`

目标读者：想把个人知识管理做起来的职场人

要求：保留原文核心观点，不要编造案例。输出每张卡片标题、正文、视觉建议、发布文案和标签，并把标准卡片包交给 `cards-to-images`。
